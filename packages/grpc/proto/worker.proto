syntax = "proto3";

package worker;

service Worker {
  rpc HandlePacket(Packet) returns (StatusReply);
}

message Packet {
  string body = 1;
}
message StatusReply {
  bool success = 1;
  optional string message = 2;
}